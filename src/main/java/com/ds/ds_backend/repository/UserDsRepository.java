package com.ds.ds_backend.repository;

import com.ds.ds_backend.model.UserDs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserDsRepository extends JpaRepository<UserDs, Integer> {
}
